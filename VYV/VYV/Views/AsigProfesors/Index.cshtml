@model IEnumerable<VYV.AsigProfesor>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Asignatura.Descripcion)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Profesor.Cedula)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Fecha)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Asignatura.Descripcion)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Profesor.Cedula)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Fecha)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.CodAsigProf }) |
            @Html.ActionLink("Details", "Details", new { id=item.CodAsigProf }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.CodAsigProf })
        </td>
    </tr>
}

</table>
